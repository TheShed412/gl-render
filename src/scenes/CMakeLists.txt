file( GLOB SCENE_SOURCES *.cpp )
add_library( scenes ${SCENE_SOURCES} )
target_link_libraries(scenes init core stb_image)
include_directories("${CMAKE_BINARY_DIR}/lib/")
target_include_directories(scenes PUBLIC ${CMAKE_CURRENT_SOURCE_DIR} INTERFACE "${CMAKE_BINARY_DIR}/lib/")